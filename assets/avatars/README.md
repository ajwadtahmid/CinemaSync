# Avatar assets

Bundled default avatar images live here (used by the profile feature in a later
phase). This README also keeps the asset directory non-empty so Flutter's asset
bundler does not error on a directory containing only hidden files.
